const Controller = require('../controller/notification') 
const express = require('express');
const router = express.Router();
//create notification
// router.get("/:customerId", Controller.getNotifications);


module.exports = router;

